# 🛡️ ServerGuard: Telegram SSH & SFTP Gatekeeper

> **Защитите ваш Linux-сервер с помощью двухфакторной аутентификации через Telegram и логирования команд в реальном времени.**

![Python](https://img.shields.io/badge/Python-3.11-blue?style=flat&logo=python)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=flat&logo=docker)
![Aiogram](https://img.shields.io/badge/Aiogram-3.x-blueviolet?style=flat&logo=telegram)
![Security](https://img.shields.io/badge/Security-Hardened-red)
![License](https://img.shields.io/badge/License-MIT-green)

**ServerGuard v2.0** — это комплексное решение для мониторинга безопасности сервера. Он превращает ваш Telegram в пульт управления доступом.

🚨 **NEW in v2.0:** Полная защита **SFTP** соединений. Теперь бот перехватывает не только терминальные сессии, но и попытки передачи файлов (FileZilla, WinSCP), блокируя доступ на уровне протокола SSH.

---

## 🔥 Возможности

### 1. 🚪 Gatekeeper (Контроль доступа)
- **Блокировка по умолчанию:** Любой вход с неизвестного IP блокируется.
- **Интерактивное подтверждение:** Бот присылает уведомление с IP, Юзером и временем входа.
- **Временный доступ:** Кнопка **[✅ Allow 1h]** открывает доступ для IP ровно на 1 час.
- **SFTP Interceptor:** Специальный wrapper подменяет подсистему SFTP, не давая скачать или загрузить файлы без авторизации.

### 2. 👁️ Command Logger (Шпион)
- **Логирование в реальном времени:** Каждая введенная команда в терминале отправляется вам в Telegram.
- **Форматированный вывод:** Сообщения приходят в красивом HTML формате.
- **Скрытность:** Работает на уровне Bash hooks.

### 3. 🛠️ ToolsServer (CLI Утилита)
- **Текстовый GUI:** Встроенная утилита для управления системой прямо из терминала.
- **История входов:** Просмотр базы данных попыток входа (IP, Статус, Время).
- **Управление:** Запуск, остановка, перезагрузка сервиса одной кнопкой.

---

## ⚙️ Установка

### Требования
- OS: Ubuntu 20.04+ / Debian 11+
- Root права
- Python 3.8+
- Docker & Docker Compose

### Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ВАШ_НИК/ServerGuard.git
   cd ServerGuard
   ```

2. **Запустите инсталлер:**
   Скрипт установит зависимости, настроит Docker и **автоматически пропатчит SSH конфиг** для защиты SFTP.
   ```bash
   sudo python3 installer.py
   ```

3. **Настройка:**
   В процессе установки введите:
   - **Telegram Bot Token** (от @BotFather)
   - **Admin ID** (ваш цифровой ID от @userinfobot)

---

## 🚀 Использование

### Как зайти через SFTP (FileZilla / WinSCP)?
1. Попробуйте подключиться.
2. Соединение **сбросится** (Exit Code 1 / Connection Closed), так как IP еще не подтвержден.
3. В Telegram придет сообщение: "🚨 UNAUTHORIZED ACCESS BLOCKED".
4. Нажмите кнопку **[✅ Allow 1h]**.
5. Попробуйте подключиться снова — теперь пустит.

### Команды Telegram Бота
- `/start` — Открыть панель администратора.
  - **🔍 Active Sessions** — Показать текущие активные SSH сессии.
  - **📜 Access History** — Показать последние 10 попыток входа.
  - **🔐 Whitelist IPs** — Список IP, которым сейчас разрешен вход.

### CLI Утилита (На сервере)
После установки введите в терминале команду:
```bash
ToolsServer
```
Меню позволяет просматривать логи, управлять Docker-контейнером и полностью удалять систему (с откатом изменений SSH).

---

## 🏗️ Архитектура v2.0

Проект использует гибридную схему перехвата:

1. **Host Layer (Bash Hooks):**
   - `/usr/local/bin/sg-check-access` — Единый скрипт проверки. Делает HTTP-запрос к локальному API бота.
   - `/etc/profile.d/z99-server-guard.sh` — Хук для интерактивных оболочек (SSH Terminal). Убивает процесс, если доступ запрещен.
   - `/usr/local/bin/sg-sftp-wrapper` — **Новое:** Обертка для SFTP-сервера.

2. **SSH Subsystem Injection:**
   Инсталлер модифицирует `/etc/ssh/sshd_config`, заменяя стандартный сервер:
   ```ssh
   # Was: Subsystem sftp /usr/lib/openssh/sftp-server
   Subsystem sftp /usr/local/bin/sg-sftp-wrapper
   ```
   Это заставляет SSH запускать наш скрипт *перед* реальным SFTP-сервером.

3. **Core Layer (Docker + Python):**
   - **Aiogram 3.x:** Бот для управления.
   - **HTTP API (8080):** Локальный интерфейс для проверки IP скриптами хоста.
   - **UDP (9999):** Прием логов команд (Fire-and-Forget).

---

## 🔒 Безопасность

- **Fail-Closed:** Если Docker-контейнер упал или API недоступен, скрипты вернут ошибку и **не пустят** никого на сервер (кроме локальной консоли).
- **Изоляция:** Бот работает в контейнере, имеет доступ только к своей БД.
- **Recovery:** Если вы случайно заблокировали себя, зайдите через VNC/IP-KVM консоль вашего хостинг-провайдера (IP определится как Localhost или пустой, и скрипт пропустит вас).

---

## 📝 Лицензия

MIT License. Вы можете свободно использовать, модифицировать и распространять этот код.
