# 🛡️ ServerGuard: Telegram SSH Monitor & Gatekeeper

> **Защитите ваш Linux-сервер с помощью двухфакторной аутентификации через Telegram и логирования команд в реальном времени.**

![Python](https://img.shields.io/badge/Python-3.11-blue?style=flat&logo=python)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=flat&logo=docker)
![Aiogram](https://img.shields.io/badge/Aiogram-3.x-blueviolet?style=flat&logo=telegram)
![License](https://img.shields.io/badge/License-MIT-green)

**ServerGuard** — это комплексное решение для мониторинга безопасности сервера. Он превращает ваш Telegram в пульт управления доступом. Если кто-то пытается зайти на сервер (SSH или SFTP), вы получаете уведомление с кнопкой "Разрешить". Если IP не подтвержден — соединение разрывается мгновенно.

---

## 🔥 Возможности

### 1. 🚪 Gatekeeper (Контроль доступа)
- **Блокировка по умолчанию:** Любой вход с неизвестного IP блокируется.
- **Интерактивное подтверждение:** Бот присылает уведомление с IP, Юзером и временем входа.
- **Временный доступ:** Кнопка **[✅ Allow 1h]** открывает доступ для IP ровно на 1 час.
- **Поддержка SFTP:** Корректно обрабатывает SFTP-клиенты (FileZilla, WinSCP), не ломая протокол передачи данных.

### 2. 👁️ Command Logger (Шпион)
- **Логирование в реальном времени:** Каждая введенная команда в терминале отправляется вам в Telegram.
- **Форматированный вывод:** Сообщения приходят в красивом HTML формате.
- **Скрытность:** Работает на уровне Bash hooks, не замедляя работу системы.

### 3. 🛠️ ToolsServer (CLI Утилита)
- **Текстовый GUI:** Встроенная утилита для управления системой прямо из терминала.
- **История входов:** Просмотр базы данных попыток входа (IP, Статус, Время).
- **Управление:** Запуск, остановка, перезагрузка сервиса одной кнопкой.

---

## ⚙️ Установка

### Требования
- OS: Ubuntu 20.04 / 22.04 / Debian 11+
- Root права
- Python 3.8+
- Установленный `git`

### Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ВАШ_НИК/ServerGuard.git
   cd ServerGuard
   ```

2. **Запустите инсталлер:**
   Скрипт сам установит Docker, создаст структуру папок и настроит службы.
   ```bash
   sudo python3 installer.py
   ```

3. **Настройка:**
   В процессе установки вас попросят ввести:
   - **Telegram Bot Token** (от @BotFather)
   - **Admin ID** (ваш цифровой ID от @userinfobot)

---

## 🚀 Использование

### Как зайти через SFTP (FileZilla / WinSCP)?
1. Попробуйте подключиться.
2. Соединение **сбросится** (Access Denied), так как IP еще не подтвержден.
3. В Telegram придет сообщение: "🚨 UNAUTHORIZED ACCESS BLOCKED".
4. Нажмите кнопку **[✅ Allow 1h]**.
5. Попробуйте подключиться снова — теперь пустит.

### Команды Telegram Бота
- `/start` — Открыть панель администратора.
  - **🔍 Active Sessions** — Показать текущие активные SSH сессии.
  - **📜 Access History** — Показать последние 10 попыток входа.
  - **🔐 Whitelist IPs** — Список IP, которым сейчас разрешен вход.

### CLI Утилита (На сервере)
После установки введите в терминале команду:
```bash
ToolsServer
```
Откроется меню:
```text
=========================================
      ServerGuard Management Tool        
=========================================
1. 📊 System Status    (Статус контейнеров)
2. 📜 View Login History (История из БД)
3. ▶️  Start/Restart Service
4. ⏹️  Stop Service
5. ❌ Uninstall        (Полное удаление)
0. 🚪 Exit
```

---

## 🏗️ Архитектура

Проект состоит из трех слоев:

1. **Host Layer (Bash):**
   - `/usr/local/bin/sg-gatekeeper` — Скрипт-блокатор. Умная проверка TTY (SSH vs SFTP).
   - `/etc/profile.d/z99-server-guard.sh` — Хук при входе. Убивает сессию (`kill -KILL`), если Gatekeeper вернул ошибку.
   - `/etc/bash.bashrc` — Перехват команд через `PROMPT_COMMAND`.

2. **Transport Layer (Network):**
   - **UDP 9999:** Прием логов.
   - **HTTP 8080:** API для проверки доступа (`/check-access`).

3. **Core Layer (Docker + Python):**
   - **Aiogram 3.x:** Асинхронный бот.
   - **Aiosqlite:** БД `guard.db` для хранения прав доступа и истории.

---

## 🔒 Безопасность

- **Fail-Safe:** Если вы случайно заблокировали себя, зайдите через VNC-консоль провайдера (IP будет определен как Localhost, и система вас пропустит).
- **Изоляция:** Бот работает в контейнере, минимизируя влияние на основную систему.

---

## 📝 Лицензия

MIT License. Вы можете свободно использовать, модифицировать и распространять этот код.
